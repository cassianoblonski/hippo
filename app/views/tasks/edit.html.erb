<div class="container">
  <div class="row">
    <div class="col-md-5 mx-auto">
      <h2 class="form-signin-heading">Editing <%= "##{@task.id} from #{@history.name}" %></h2>
      <%= render 'form' %>
    </div>
  </div>
</div>
